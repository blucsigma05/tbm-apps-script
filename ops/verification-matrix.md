# TBM Verification Matrix — CP-3
<!-- control-plane v1 — sourced from CLAUDE.md Deploy Pipeline, QA Gates, and actual repo change patterns -->
<!-- each row = one change type; each column = the verification required at that layer -->

| Change Type | Verify Main (source + editor) | Verify Deployed (GAS/CF endpoint) | Verify Device (real hardware) | Verify Operational (post-deploy monitoring) | Close Only When |
|---|---|---|---|---|---|
| **Education payload/seed shape** (Curriculum seed, getTodayContent shape, question fields) | Grep source for exact field names; run getTodayContentSafe('buggsy') and getTodayContentSafe('jj') from editor; Logger output shows: question, options, answer, teks, id, difficulty all present with correct types | `?action=runTests` → `overall == "PASS"` AND `smoke.overall == "PASS"`; call `/api?fn=getTodayContentSafe&args=["buggsy"]` via curl — confirm shape matches spec; CF routes `/homework`, `/reading`, `/writing` return 200 | Homework renders question with answer choices on Buggsy tablet (no "undefined"); SparkleLearning renders first question on JJ tablet | ErrorLog clean (no new entries) since deploy; no education Pushover alerts fired | Device verified by LT [awaiting-device-verify] flagged |
| **Kid renderer / HTML surface** (any .html file serving Buggsy, JJ, or Parent) | Browser DevTools on built HTML: layout renders without broken elements; grep .html for ES5 compliance (no `=>`, `let`, `const`, template literals, `?.`, `??`); posttool-write.sh shows no violations | CF route for the surface returns 200; rendered HTML has no `undefined` or `null` text visible; browser console shows no JS errors; audit-source.sh PASS | QA walk on actual device: correct layout at device viewport, touch targets reachable, no broken rendering, all buttons trigger expected actions | ErrorLog clean; no KH Pushover alerts from the surface | Device walk by LT [awaiting-device-verify] flagged |
| **Dashboard / finance formatting** (ThePulse, TheVein, TheSpine, TheSoul data display) | Logger output from getDataSafe or relevant getData* call matches expected KPI values; source grep confirms no client-side calculations introduced | `?action=runTests` PASS; ThePulse and TheVein render in browser without broken values; `getDeployedVersionsSafe` returns correct version; CF `/pulse` and `/vein` return 200 (behind PIN gate) | JT verifies ThePulse on S25 — values look correct; LT verifies TheVein on desktop; TheSpine renders on Fire Stick | No finance ErrorLog entries; no ThePulse/TheVein Pushover alerts; Tiller freshness check (HYG-09) not triggered | LT device-verified; JT confirms ThePulse |
| **Alert routing** (AlertEngine.js, sendPush_ changes, PUSHOVER_PRIORITY constants, recipient routing) | Grep confirms all sendPush_ callers use named PUSHOVER_PRIORITY constants (no bare integers); correct recipient passed (LT / JT / BOTH); test sendPush_ from editor — Logger confirms API call made | Production test alert delivered to Pushover; check Pushover history confirms message received by correct recipient | Pushover notification received on LT / JT phone (as expected per routing) | No false alerts fired in 24h after deploy; no unintended silenced alerts | LT + JT confirm test push received |
| **Deploy / freeze / mutation path** (audit-source.sh, DeployGate, diagPreQA, GASHardening, TBMConfig changes) | `bash audit-source.sh` exits 0 (all PASS); `diagPreQA()` from GASHardening.gs: ALL categories PASS; version numbers match in all 3 locations (header, getter, EOF) per changed .gs file | `?action=runTests` PASS; smoke.overall PASS; `getDeployedVersionsSafe` returns bumped version numbers; ErrorLog has no new entries | (not device-specific — infra change) | ErrorLog clean for 60 min post-deploy; no deploy-fail Pushover | Smoke PASS + version confirmed deployed + ErrorLog clean |
| **Automation / scheduled task** (AuditTrigger, CalendarSync, scheduled dailyHealthCheck, resetDailyTasksAuto) | Run function manually from GAS editor; Logger shows expected output (no errors, correct data); verify it writes/reads the correct tabs | Trigger fires on GAS time-based schedule; check Execution Log for green run; check ErrorLog for no new entries from the function | (not device-specific — scheduled tasks) | Zero errors in ErrorLog from trigger runs across 3 consecutive scheduled fires | Manual trigger verified PASS + one scheduled run observed without error |
| **Shared utility / helper** (GASHardening, AlertEngine, Utility.gs, pattern-registry function change) | Read all callers in the file map; grep for call sites; editor test of representative callers; confirm shared globals not redeclared | `?action=runTests` PASS; all surfaces that call the utility return 200 | All affected surfaces tested on device (if user-visible) | ErrorLog clean; no regressions in PerfLog | Device verify of all surfaces that call the utility; smoke PASS |
| **New route or surface** (adding HTML file + CF route + Code.js servePage entry + shim FNS registration) | Code.js servePage routes table has new entry; cloudflare-worker.js PATH_ROUTES has new entry; FNS array in shim has all new google.script.run calls; ES5 compliance verified; `ops/surface-map.md` updated in same PR | New CF route returns 200; GAS htmlSource serves the HTML without error; `/api?fn=<newFn>` returns expected JSON | New surface renders on target device at correct viewport | ErrorLog clean for new surface; no CF 500s | Device verified + surface-map.md updated in same PR (Work Doctrine rule 14) |
| **Skill creation or extension** (adding or modifying a claude-code skill file) | Skill file reviewed against CLAUDE.md contracts; no hallucinated function/file names — all references grepped in repo; schema fields match actual data shapes | (skills are not GAS-deployed — no GAS verify step) | (not device-specific) | No context errors on skill invocation; skill produces accurate output on first real task run | LT reviews skill output on 1 real task; no fabricated claims in skill output |
| **KidsHub mutation** (khCompleteTask, khApproveTask, khAddBonus, khRedeemReward, any KH write path) | Read KH write function source; confirm stampKHHeartbeat_() called after write; confirm waitLock(30000) used (not tryLock); Logger output shows correct tab written | `?action=runTests` PASS; POST to `/api?fn=<mutationFn>Safe` returns expected result; KH heartbeat (Helpers!Z1) updates in sheet | Parent Hub shows updated state on S25; kid tablet reflects change (chore marked done, ring balance updated) | ErrorLog clean; KH heartbeat age < 120s in cache; no KH Pushover alerts | Device-verified: LT or JT confirms mutation visible on both kid tablet and Parent Hub |
| **Finance mutation / month-end close** (stampCloseMonth, reconcileVeinPulse, runMERGates) | Run in editor with test data; Logger shows all MER gates evaluated; Close History row written correctly; no bare sheet name strings (all via TAB_MAP) | `getMERGateStatusSafe` returns correct gate states; `stampCloseMonthSafe` writes Close History; `?action=runTests` PASS | LT verifies TheVein close workflow renders new close entry | ErrorLog clean; Close History has new row; no PROD_DOWN Pushover | LT confirms close entry visible in TheVein |
| **Cloudflare Worker change** (cloudflare-worker.js — routes, shim, PIN gate, QA isolation) | Wrangler config valid (`wrangler.toml`); all PATH_ROUTES accounted for in surface-map.md; FNS array complete; QA_ROUTES mirrors prod routes minus finance; QA_DENIED explicitly blocks finance surfaces | `wrangler deploy` completes; `/version` returns new WORKER_BUILD; all PATH_ROUTES return 200 via curl; PIN gate sets cookie on correct POST; `/qa/homework` returns QA amber banner | Finance surface requires PIN on fresh session; kid tablet routes open without PIN | No CF error logs; no surface reports 500 or failed shim calls | LT verifies PIN gate + all critical routes post-deploy |
